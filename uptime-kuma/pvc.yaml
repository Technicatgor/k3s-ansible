apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-data-claim
  namespace: uptime-kuma
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
